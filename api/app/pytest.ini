[pytest]
pythonpath = .

# fix DeprecationWarning bug in testing
filterwarnings =
    ignore::DeprecationWarning:httpx.*:
    ignore::DeprecationWarning:passlib.*:
    ignore::PendingDeprecationWarning:starlette.*:
